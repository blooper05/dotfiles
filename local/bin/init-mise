#!/bin/bash

set -ex

mkdir -p "$HOME/.local/share/gnupg"

/opt/homebrew/bin/mise install

rm -rf "$HOME/.cargo/"
rm -rf "$HOME/.rustup/"

claude mcp add --scope user --transport sse mcp-hub http://localhost:37373/mcp
